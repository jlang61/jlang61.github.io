import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faLinkedin } from '@fortawesome/free-brands-svg-icons';

const LinkedInIcon = () => {
  return (
    <FontAwesomeIcon icon={faLinkedin} className="text-gray-200" />
  );
};

export default LinkedInIcon;
